COMPOSE_PROJECT_NAME=biznest
FRONTEND_URL=http://localhost:3001

# POSTGRES CONFIGURATION
POSTGRES_USERNAME=biznest
POSTGRES_PASSWORD=biznest123
POSTGRES_DB=biznest
POSTGRES_HOST=localhost
POSTGRES_PORT=5437


# MINIO CONFIGURATION
MINIO_ROOT_USER=biznest
MINIO_ROOT_PASSWORD=biznest123
MINIO_HOST=localhost
MINIO_PORT=9007
MINIO_WEB_PORT=9091
MINIO_BUCKET=upload

# Redis CONFIGURATION
REDIS_HOST=localhost
REDIS_PORT=6381
REDIS_DB=0

# JWT CONFIGURATION
SECRET_KEY=<generate_your_own_secret_key>
ALGORITHM=HS256
JWT_COOKIE_NAME=ACCESS_TOKEN
SECURE_COOKIES=false
REFRESH_TOKEN_EXPIRE_SECONDS=604800
REFRESH_COOKIE_NAME=REFRESH_TOKEN
ACCESS_TOKEN_EXPIRE_SECONDS=900

# External API Tokens
HUGGING_FACE_TOKEN=<hugging_face_token>
PSGC_API_TOKENN=<psgc_token>
ANTHROPIC_API_KEY=

# SMTP — leave blank to disable email (magic_link still returned in API response)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
LGU_INVITE_EXPIRE_HOURS=24